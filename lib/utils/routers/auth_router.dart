import 'package:go_router/go_router.dart';

/// A router for authentication-related routes.
///
/// This router currently has no defined routes.
/// It serves as a placeholder for future authentication routes.
final GoRouter authRouter = GoRouter(
  initialLocation: '/',
  routes: <RouteBase>[

  ],
);
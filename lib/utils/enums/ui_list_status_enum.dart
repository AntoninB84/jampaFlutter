/// This enum represents the status of a UI list component.
/// TODO actually use this enum in the UI list components to manage their states.
enum UIListStatusEnum {
  loading,
  success,
  empty,
  error,
}
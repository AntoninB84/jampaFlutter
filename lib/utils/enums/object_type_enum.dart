/// This enum defines various object types used in the application.
/// Each value corresponds to a specific entity type.
///
/// This is useful for retrieving payload data from notifications and alarms.
enum ObjectTypeEnum {
  categoryEntity,
  noteEntity,
  noteCategoryEntity,
  noteTypeEntity,
  reminderEntity,
  scheduleEntity,
  userEntity;
}